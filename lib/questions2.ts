// Question-bank access for Assessment 2 (the Capgemini 2027 mock test).
// Same shape / rules as lib/questions.ts — MCQ answers are stored as option
// text so per-session option shuffling never breaks correctness.
import data from '@/data/questions2.json'

export const bank2: any = data
